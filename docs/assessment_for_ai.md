# 机器人实战题：多来源机器人示教数据的统一入库与质检

## 背景

我们需要把来自不同来源的机器人操作数据，整理成一套可以直接喂给模型训练的数据集。

麻烦在于这些数据彼此不一致：不同的机器人本体（单臂、双臂、移动底盘）、不同的动作维度和物理含义、不同的采集帧率、不同的相机路数和分辨率、有的来自真机有的来自仿真、质量参差不齐。上游还会持续有新的数据集进来。

你要做的是一条能长期跑的服务：持续从上游拉取新数据，归一化到统一表示，做质量检查，存进本地库，并按训练需求导出子集。

## 数据源

公开可下载、不需要申请权限的具身 / 机器人数据集。核心要求是**跨越至少两种不同的存储格式**，并且至少包含一个"非标准机器人单臂"的来源——差异越大，你的统一归一化设计越有说服力。

**请至少选 3 个彼此差异明显的来源**，且至少覆盖两种存储格式、包含一个非单臂来源：

1. **LeRobot 格式（Parquet + MP4）**：入口 `https://huggingface.co/datasets?other=LeRobot`，或直接看 `lerobot` 组织下的数据集，例如 `lerobot/pusht`、`lerobot/aloha_sim_insertion_human`、`lerobot/aloha_sim_transfer_cube_human`。
   - 目录结构统一：`meta/info.json`（本体类型、帧率、总 episode 数、各字段的 dtype 和 shape）、`meta/episodes/`、`meta/tasks.parquet`、`data/`（parquet，逐帧的 action / state / timestamp / episode_index 等）、`videos/`（各路相机的 mp4）。
   - 可用 `lerobot` 库直接加载，也可用 `datasets` / `huggingface_hub` 拉原始文件自行解析。
2. **一个 RLDS / TFDS 格式的 Open X-Embodiment 子集**：例如 `jxu124/OpenX-Embodiment` 里体量较小的一个 sub-dataset，或 `bridge` / `fractal` 的小切片。它是 episode → steps 的嵌套结构，动作空间约定也和 LeRobot 不同——正好逼你面对"同一件事，不同人怎么存"。
3. **一个异构格式 / 非标准单臂的来源（任选其一）**：
   - 人类第一视角小样本（如 Ego4D / EPIC-KITCHENS 的少量片段）：没有 action、没有本体感知，只有视频（可能带手部 / 交互标注）——考你的 schema 能不能优雅降级，而不是塞占位值或直接报错。
   - 或一个 HDF5 格式的机器人集（robomimic / ALOHA 类）：文件组织和 action 语义都和上面不同。

- 三个来源之间：本体不同、动作维度不同、帧率不同、相机路数不同，最好真机和仿真都有。
- 建议总量控制在十几万帧以内，个人电脑能处理。视频可以选择性下载；如果只处理低维信号不下视频，请在文档里说明这个取舍和它的代价。

## 任务

### 1. 设计并实现一条增量的数据入库 pipeline，包含以下环节

**a. 数据获取**

- 从上游拉取数据集，逐 episode 处理。
- 支持增量：记录已处理的范围，下次运行只处理新增的 episode 或新增的数据集，不重复处理。

**b. 统一表示与归一化**

- 设计一套你自己的统一 schema，把不同来源的 episode 映射进去。
- 需要在文档里说清楚：哪些信息必须无损保留、哪些可以有损或丢弃、为什么；动作维度和物理含义不同的本体，你怎么处理（统一到一个空间、还是分组保留并打标签）；帧率不同、存储格式不同（Parquet / RLDS / HDF5 / 纯视频）怎么统一读取。
- 缺模态的来源要能优雅降级：像人类第一视角这种没有 action、没有本体感知的数据，schema 里用可空字段 + 能力声明来表示，而不是塞占位值或直接报错。
- 这一环是本题的核心，先想清楚再动手。

**c. 质量检查**

- 定义并实现至少 4 条质量检查规则，针对机器人示教数据的真实坏法，不要写成通用的空值检查。
- 可以考虑的方向（不限于此，也不要求全做）：动作值超出合理范围或帧间突变；时间戳非单调、跳变、与标称帧率对不上；视频帧数与 parquet 行数不一致；episode 过短或几乎全程静止；某路相机缺失或画面异常；state 与 action 长期完全一致这类采集异常。
- 不合格数据单独记录并给出原因，不阻塞整体流程。

**d. 结果存储**

- 存入本地数据库（SQLite 即可）。
- schema 要能区分：合格、不合格、需要人工复核。
- 支持增量写入，新一轮结果追加到已有数据上。

**e. 断点恢复**

- pipeline 中途中断后（下载失败、程序崩溃等），重启能从中断处继续，不重复处理已完成的部分。
- 需要考虑：下载了但还没归一化的、归一化了但还没质检的、质检了但还没写库的，各阶段的中间状态怎么处理。

**f. 训练子集导出**

- 给定一个总帧数预算（例如 5 万帧），从合格数据里选出一个训练子集。
- 输出为 JSON Lines，包含：来源、本体、episode 标识、帧范围、归一化后的关键字段、质检结论。
- **在文档里说明你的采样策略和理由**：预算有限时，你按什么原则分配给不同来源和本体？多样性和数据量怎么权衡？这一问没有标准答案，看的是判断。

### 2. 每次运行输出处理报告

- 本轮：新增 episode 数、归一化成功/失败、质检通过/未通过（按规则分类计数）。
- 累计：总 episode 数、总帧数、按来源和本体的分布、各质检规则的命中率。

### 3. 在文档中说明

- pipeline 整体架构和各环节职责划分。
- 统一 schema 的设计和取舍。
- checkpoint 设计：存什么状态、存在哪、如何保证幂等。
- 你如何测试了断点恢复（模拟了什么故障、恢复后行为是否符合预期）。
- 采样策略的理由。
- 如果要把这条 pipeline 部署成长期运行的生产服务，还需要考虑什么。

## 验收场景

我们会 clone 你的 repo，跑一遍处理若干个 episode，在质检阶段 kill 进程，然后重启 pipeline，期望它从断点继续而不是从头开始；随后再跑一次，期望它识别出没有新增数据、不重复入库。请确保你的代码支持这两个场景。

## 考察方向的优先级（从高到低）

1. 统一表示与跨本体归一化的设计（最核心）
2. 质检规则的针对性，是否真的理解这类数据会怎么坏
3. 增量与断点恢复
4. 训练子集采样策略的判断
5. 工程取舍与代码质量

上面五项是技术层面的。此外还有一个平行维度，AI 使用，独立考察，单独有节。

## 架构扩展问题（在文档中回答）

如果数据规模从 3 个数据集十几万帧，涨到 500 个数据集、5 亿帧，并且训练侧要求能按帧随机读取，你的设计哪些地方需要改？怎么改？

## 要求

- 使用 git 管理代码，要求有意义的 commit message，能体现开发过程，不接受单次大提交。commit 频率应与 AI 对话记录的时间线大致对应（评审方会对照 commit 和对话记录看你的迭代节奏）。
- 交付物：包含 `.git/` 的完整项目目录（打包或 git repo 链接均可）+ 文档（架构设计、schema 取舍、checkpoint 策略、断点恢复测试、采样策略、已知局限、架构扩展讨论）

## 说明

- 数据库用 SQLite 即可，不需要部署独立数据库服务。
- 不需要 GPU，不需要训练模型，本题不考察模型能力。
- 如果某个数据集下载不便，换成同类型的其他公开数据集即可，不影响评估。
